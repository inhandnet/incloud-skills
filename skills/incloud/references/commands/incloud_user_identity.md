## incloud user identity

Manage user identities across organizations

### Synopsis

View the current user's identities (roles) across different organizations.

### Options

```
  -h, --help   help for identity
```

### Options inherited from parent commands

```
      --context string   Override active context (env: INCLOUD_CONTEXT)
      --debug            Enable debug output (env: INCLOUD_DEBUG)
      --jq string        Filter JSON output using a jq expression (implies -o json)
  -o, --output string    Output format: json, table, yaml (default: table for TTY, json otherwise)
      --tenant string    Switch organization context by ID (env: INCLOUD_TENANT)
```

### SEE ALSO

* [incloud user](incloud_user.md)	 - Manage users
* [incloud user identity list](incloud_user_identity_list.md)	 - List identities across organizations

